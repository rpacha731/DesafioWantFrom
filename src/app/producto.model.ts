export class Producto {
    constructor(public id: number, public nombre: string, public precio: number, public categoria: string, public marca: string, public estado: boolean, public destacado: boolean) { };
}